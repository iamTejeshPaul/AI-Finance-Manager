specVersion: "v1beta"
serviceId: "finance-manager"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finance-manager-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
